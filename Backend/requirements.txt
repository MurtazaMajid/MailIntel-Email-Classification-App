# Core API
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0

# Gmail API
google-api-python-client>=2.130.0
google-auth-httplib2>=0.2.0
google-auth-oauthlib>=1.2.0

# Supabase
supabase>=2.4.0

# ML — pre-trained models
scikit-learn>=1.4.0
joblib>=1.4.0
numpy>=1.26.0

# TensorFlow — use slim CPU-only build to keep deploy size manageable on Render
# If you have a GPU instance, replace with: tensorflow>=2.16.0
tensorflow-cpu>=2.16.0

# Utilities
python-dotenv>=1.0.0
